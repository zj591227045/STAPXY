# 代理路由配置
# 格式: siteId:subdomain:targetUrl,siteId2:subdomain2:targetUrl2
PROXY_ROUTES=site1:a1.yourdomain.com:http://localhost:3001,site2:a2.yourdomain.com:http://localhost:3002

# Vercel KV配置（仅在Vercel环境中需要）
# 这些变量会在Vercel部署时自动设置
# KV_REST_API_URL=your_kv_rest_api_url
# KV_REST_API_TOKEN=your_kv_rest_api_token

# Next.js运行时配置
# 在本地开发时设置为 'nodejs'，在Vercel Edge Functions中自动设置为 'edge'
# NEXT_RUNTIME=nodejs

# 环境标识
NODE_ENV=development

# Vercel环境标识（在Vercel部署时自动设置）
# VERCEL=1
